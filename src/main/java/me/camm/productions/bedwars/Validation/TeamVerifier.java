package me.camm.productions.bedwars.Validation;

public class TeamVerifier
{
    //this package would be used for verifying that the info in the setup files don't conflict
    /*
    e.g
    if the shopkeepers are in air, not a wall
    if the teams and gens are within the boundaries
    if there aren't any conflicts with registration
    if the chests and spawns are valid
    if the void level is valid

    Do this after the critical plugin code is done

     */
}
